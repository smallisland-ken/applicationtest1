<body class="editbody">
<h1>Editing Book</h1>

<%= form_with model:@book, url:"/books/#{@book.id}", local: true do |f| %>
<!--urlの部分か記載されてないとデータを引き継いで表示できないのはなぜ？
　railsを学ぼうの6章ではurlがあるのに、アプリを完成させようの6章にはない..？なぜ？-->
  <%# ここからエラーメッセージ %>
    <% if @book.errors.any? %>
    <div id="error_explanation">
      <div class="error"><%= pluralize(@book.errors.count, "error") %> prohibited this product from being saved:</div>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%# ここまでエラーメッセージ %>
  <%# ここまでエラーメッセージ %>
　  <div class="div">
    <div>Title</div>
    <%= f.text_field :title %>
  </div>

  <div class="div">
  <div>Body</div>
  <%= f.text_area :body %>
 </div>
 <div class="update"><%= f.submit 'Update Book' %></div>

<% end %>
 <%= link_to "Show", book_path(@book) %> |  <%= link_to "Back", "/books" %>
</body>